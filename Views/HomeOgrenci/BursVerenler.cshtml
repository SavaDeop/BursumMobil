@model IEnumerable<BursumMobil.Models.BursVeren>
@{
    ViewBag.Title = "BursVerenler";
    Layout = "~/Views/Shared/_LayoutPage1.cshtml";
}


<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>

    @*<table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.KurumAdi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Aciklama)
            </th>
            <th></th>
        </tr>*@
    <div class="yenigenel"
      <div class="album py-5 bg-light">
        <div class="container">
            <div class="row">
                @foreach (var item in Model)
                {
                    <div class="col-md-4">
                        <div class="card mb-4 shadow-sm">
                            <svg width="100%" height="225"
                                 xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
                                <image href="~/images/alku-logo.png" height="100%" width="100%" />
                            </svg>
                            <div class="card-body">
                                @*<p class="card-text">@Html.DisplayFor(modelItem => item.KurumAdi)</p>*@
                                <div class="d-flex justify-content-between align-items-center">
                                    @*<div class="btn-group btn-sm btn-adn " >
                                            @Html.ActionLink("Detay", "Details", new { id = item.Id })

                                        </div>*@
                                    <small class="text-muted"></small>
                                </div>
                            </div>
                        </div>
                    </div>
           
    









                    @*<td>
                            @Html.DisplayFor(modelItem => item.KurumAdi)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Aciklama)
                        </td>
                        <td>
                            @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                            @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                        </td>*@

                }
            </div>
        </div>
    </div>
    </div>

    @*</table>*@
</body>
</html>


